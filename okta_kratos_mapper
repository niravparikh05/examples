local claims = std.extVar('claims');

{
  identity: {
    traits: {
      email: claims.email,
      first_name: claims.first_name,
      last_name: claims.last_name,
      group_name: claims.groupName
    },
  },
}
